# 재무관리 시스템 — 사용 설명서

투자 중심 **2인 가구**를 위한 Google Sheets 재무관리 템플릿입니다.  
일별 지출·수입, 보유 종목, 부채, 세금 한도, DSR, 시나리오까지 **한 스프레드시트**에서 관리합니다.

> **처음이신가요?** 아래 [처음 배포하기 (0→1)](#처음-배포하기-01) 섹션만 순서대로 따라 하시면 됩니다.

---

## 목차

1. [이 시스템으로 할 수 있는 것](#이-시스템으로-할-수-있는-것)
2. [처음 배포하기 (0→1)](#처음-배포하기-01)
3. [배포 직후 10분 설정](#배포-직후-10분-설정)
4. [매일·매주·매월 루틴](#매일매주매월-루틴)
5. [시트별 설명 (19개 탭)](#시트별-설명-19개-탭)
6. [자산_종목 입력 가이드](#자산_종목-입력-가이드)
7. [예산 · 비상자금 · 목표](#예산--비상자금--목표)
8. [자산 이동 · 템플릿](#자산-이동--템플릿)
9. [시세 · 환율 · 일별 자산 기록](#시세--환율--일별-자산-기록)
10. [재무관리 메뉴 (Apps Script)](#재무관리-메뉴-apps-script)
11. [자동 실행 (트리거)](#자동-실행-트리거)
12. [월간 리포트 (Google Docs)](#월간-리포트-google-docs)
13. [다른 컴퓨터에서 GitHub 받기 · Google 연동](#다른-컴퓨터에서-github-받기--google-연동)
14. [문제 해결 (FAQ)](#문제-해결-faq)
15. [보안 · 재배포 주의](#보안--재배포-주의)

---

## 이 시스템으로 할 수 있는 것

| 영역 | 기능 |
|------|------|
| **일상** | 2인 수입·지출 매일 기록, 카테고리별 **예산 vs 실적** |
| **투자** | ISA·연금·주식·해외(USD/CNY) 종목별 평가, **담당자**별 자산 |
| **부채** | 대출 등록, DSR·월상환, 순자산(자산−부채) |
| **계획** | 목표 순자산·예상 달성, 시나리오(지금 vs 1년 후) |
| **세금·만기** | ISA/연금 한도, 만기 D-day 알림 |
| **자동화** | 9시 시세·환율·일별 스냅샷, 23시 월말 리포트 |

**시작 화면은 항상 `한눈에보기` 탭**입니다. 상세 숫자는 마지막 탭 `대시보드(상세 KPI)`에서 확인합니다.

---

## 처음 배포하기 (0→1)

아직 Google Sheets에 **한 번도 올리지 않았다면** 아래 순서대로 진행하세요.  
**총 소요: OAuth 설정 5~10분 + 배포 1~2분**

### 준비물

| 항목 | 설명 |
|------|------|
| PC | Windows / Mac (배포할 때만 필요) |
| Python | **3.10 이상** — [python.org](https://www.python.org/downloads/) |
| Google 계정 | Gmail 또는 Google Workspace |
| 인터넷 | OAuth·API 호출용 |

### Step 0. GitHub에서 받기 (다른 PC·처음 clone)

이미 누군가 저장소에 올려 두었다면, **Git clone**으로 받을 수 있습니다.

**저장소:** [github.com/moonhyun-cheol/money-morny](https://github.com/moonhyun-cheol/money-morny)

```powershell
# 원하는 폴더에서
git clone https://github.com/moonhyun-cheol/money-morny.git
cd money-morny
```

> Git이 없으면 [git-scm.com](https://git-scm.com/download/win) 설치 후 위 명령 실행,  
> 또는 GitHub 페이지 **Code → Download ZIP** 으로 받아 압축 해제해도 됩니다.

clone 후 폴더 구조:

```
money-morny/
├── setup.py
├── requirements.txt
├── config/
│   ├── credentials.json.example   ← 참고용
│   └── credentials.json           ← 직접 넣어야 함 (Git에 없음)
├── deploy/
├── apps-script/
└── docs/사용설명서.md
```

### Step 0-1. 프로젝트 폴더 확인 (로컬 경로 예시)

```
c:\Users\Temp\Desktop\현철\코딩연습\재무관리 프로그램\
```

### Step 1. Google Cloud 프로젝트 만들기

1. [Google Cloud Console](https://console.cloud.google.com) 접속
2. 상단 **프로젝트 선택 → 새 프로젝트** (이름 예: `재무관리`)
3. 생성된 프로젝트를 선택한 상태로 진행

### Step 2. API 4개 켜기

[API 라이브러리](https://console.cloud.google.com/apis/library)에서 검색 후 **사용** 클릭:

1. **Google Sheets API**
2. **Google Docs API**
3. **Google Drive API**
4. **Google Apps Script API**

> 하나라도 빠지면 배포 중 `403 API not enabled` 오류가 납니다.

### Step 3. OAuth 동의 화면

1. **API 및 서비스 → OAuth 동의 화면**
2. User Type: **외부** (개인 Gmail OK)
3. 앱 이름: `재무관리` (아무 이름 가능)
4. 사용자 지원 이메일·개발자 연락처 입력 → 저장
5. **범위(Scopes)** 단계는 건너뛰어도 됨
6. **테스트 사용자** → **+ ADD USERS** → **배포에 쓸 Gmail 주소** 추가  
   → 저장

> 테스트 모드에서는 **등록한 Gmail만** 로그인할 수 있습니다.

### Step 4. OAuth 클라이언트 ID (데스크톱)

1. **API 및 서비스 → 사용자 인증 정보**
2. **+ 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**
3. 애플리케이션 유형: **데스크톱 앱**
4. 이름: `재무관리-local` → 만들기
5. **JSON 다운로드**
6. 다운로드한 파일을 아래 경로로 복사·이름 변경:

```
재무관리 프로그램\config\credentials.json
```

`credentials.json.example`은 참고용이며, **실제 JSON 내용**으로 교체해야 합니다.

### Step 5. Python 패키지 설치

**PowerShell** 또는 **명령 프롬프트**에서:

```powershell
cd "c:\Users\Temp\Desktop\현철\코딩연습\재무관리 프로그램"
pip install -r requirements.txt
```

Python이 `python`이 아니라 `py`로만 잡히면:

```powershell
py -3 -m pip install -r requirements.txt
```

### Step 6. 배포 실행

```powershell
cd "c:\Users\Temp\Desktop\현철\코딩연습\재무관리 프로그램"
python setup.py deploy
```

(`python`이 안 되면 `py -3 setup.py deploy`)

**진행 순서 (자동):**

| 단계 | 동작 |
|------|------|
| OAuth | 브라우저 열림 → Google 로그인 → **권한 허용** |
| 1/4 | Google Docs 월간 리포트 **템플릿** 생성 |
| 2/4 | Google Sheets **스프레드시트 19탭** 생성 |
| 3/4 | Apps Script 코드 업로드 (메뉴·시세·자동화) |
| 4/4 | URL 출력 |

**성공 시 터미널 예시:**

```
============================================================
재무관리 시스템이 생성되었습니다.
============================================================
  Spreadsheet : https://docs.google.com/spreadsheets/d/xxxxx/edit
  Docs 템플릿 : https://docs.google.com/document/d/xxxxx/edit
  Apps Script : https://script.google.com/home/projects/xxxxx/edit
```

1. **Spreadsheet URL**을 메모장·즐겨찾기에 저장하세요.  
   → 이후 매일 이 링크만 열면 됩니다.

2. `config\token.json`이 자동 생성됩니다.  
   → 같은 PC에서 **재배포**할 때 브라우저 없이 로그인될 수 있습니다.

### Step 7. 스프레드시트 첫 실행

1. 출력된 **Spreadsheet URL** 클릭
2. **F5 새로고침** (메뉴가 안 보일 때)
3. 상단에 **「재무관리」** 메뉴 확인
4. **재무관리 → ⚙ 트리거 설치 (자동)** 클릭
5. **Apps Script 권한** 최초 1회 승인 (Google 보안 경고 → 고급 → 허용)

> 트리거를 설치해야 **매일 9시 시세·일별 자산**, **23시 월말**이 동작합니다.

---

## 배포 직후 10분 설정

### 1. `설정` 시트

| 항목 | 예시 | 설명 |
|------|------|------|
| Person1 이름 | `현철` | 수입·지출·**담당자** 열과 **일치**해야 함 |
| Person2 이름 | `○○` | 배우자 이름 |
| 목표 순자산 | `100000000` | 원 단위 (1억) |
| 추세 계산 개월수 | `6` | 목표 달성 예측용 |
| **예산 (월 한도)** | 식비 60만 등 | 카테고리별 지출 상한 |

### 2. `자산_종목` 시트

- 샘플 4행 참고 후 본인 계좌·종목으로 수정
- **C열 담당자** = 설정의 Person1/Person2 이름 또는 `공동`
- **계좌명** 규칙: `사람1-키움ISA` (같은 통장은 같은 이름)

### 3. `부채` 시트 (대출 있으면)

- 담보대출·신용대출 등 **현재잔액·월상환액** 입력

### 4. 시세 첫 갱신

**재무관리 → 📊 갱신 → 시세+환율+일별자산**

- 해외 주식·환율 반영
- `자산_일별이력`에 **오늘** 첫 행 기록

### 5. `한눈에보기` 확인

- 순자산·순저축·DSR·목표·**비상자금(개월)** KPI
- 담당자별 자산 도넛, 예산 vs 실적 차트

---

## 매일·매주·매월 루틴

### 매일 (1~3분)

| 할 일 | 시트 |
|------|------|
| 지출 입력 | `지출` — 날짜, 카테고리, 금액, 담당자 |
| 수입 있으면 | `수입` — 급여·배당 등 |
| (선택) KPI 확인 | `한눈에보기` |

### 주 1회

| 할 일 | 방법 |
|------|------|
| 시세·환율 갱신 | 메뉴 **시세+환율+일별자산** (9시 자동도 있음) |
| 자산 수정 | `자산_종목` — 매수·매도·입금 반영 |

### 월 1회

| 할 일 | 방법 |
|------|------|
| 예산 한도 점검 | `설정` 또는 `월간집계` 예산 vs 실적 |
| 월말 스냅샷 | 23시 **자동** 또는 메뉴 **월말 스냅샷** |
| 월간 리포트 | 메뉴 **월간 리포트 생성** → Docs PDF/공유 |

---

## 시트별 설명 (19개 탭)

| 탭 | 역할 | 자주 쓰나? |
|----|------|-----------|
| **한눈에보기** | **메인 허브** — KPI·차트·탭 링크 | ★★★ |
| 설정 | 이름·목표·예산·Docs ID | 초기 1회 |
| 자산_종목 | 계좌·종목·담당자·평가 | ★★★ |
| 지출 | 일별 지출 | ★★★ |
| 수입 | 2인 수입 | ★★★ |
| 월간집계 | 이번 달 자동 합계·**예산 vs 실적** | 확인용 |
| 순자산_이력 | **월별** 스냅샷·추세 차트 | 자동 |
| 시세 | 종목 시세 캐시 | 자동 |
| 자산_이동 | 큰 돈 이동 계획 | 가끔 |
| 부채 | 대출·잔액 | ★★ |
| 세금한도 | ISA·연금 연간 한도 | ★★ |
| 만기_캘린더 | ISA·적금 만기 D-day | ★★ |
| DSR_상환 | DSR·월상환·가용여력 | ★★ |
| 시나리오 | 지금 vs 1년 후 순자산 | 계획 시 |
| 환율 | USD·CNY→KRW | 자동 |
| 이동_템플릿 | ISA→연금 등 원클릭 | 가끔 |
| 계좌목록 | 담당자·통장별 자동 집계 | 확인용 |
| 자산_일별이력 | **일별** Person1/2/합계·선그래프 | ★★ |
| **대시보드** | 상세 KPI (마지막 탭) | 필요 시 |

---

## 자산_종목 입력 가이드

### 열 구조 (A~N)

| 열 | 필드 | 입력 팁 |
|----|------|---------|
| A | 계좌유형 | 드롭다운 (ISA, 적금, 주식…) |
| B | 계좌명 | `사람1-키움ISA` 형식 권장 |
| **C** | **담당자** | 설정 B2/B3 이름 또는 `공동` |
| D | 티커 | `005930`, `VOO` (없으면 비움) |
| E | 종목명 | 표시용 |
| F | 수량 | 주식=주수, 적금=1 |
| G | 평단가 | 해당 **통화** 기준 |
| H | 현재가 | 시세 갱신 또는 수동 |
| I | 평가금액(원) | **자동** = F×H×환율 |
| L | 시장 | KS / KQ / US / CN |
| M | 통화 | KRW / USD / CNY |

### 국내 주식 예

| A | B | C | D | L | M |
|---|---|---|---|---|---|
| ISA | 사람1-키움ISA | 사람1 | 005930 | KS | KRW |

### 미국 ETF 예

| D | L | M | G·H 단위 |
|---|---|---|----------|
| VOO | US | USD | 달러 |

### 적금·예금 예

| A | D | F | H | M |
|---|---|---|---|---|
| 적금 | (비움) | 1 | 5000000 | KRW |

> 수량=1, 현재가=잔액(원)으로 두면 됩니다.

---

## 예산 · 비상자금 · 목표

### 예산 vs 실적

1. **`설정`** 하단 **「예산 (월 한도)」** — 카테고리별 월 한도(원)
2. **`지출`**에 매일 입력
3. **`월간집계`** — 한도 / 실적 / 달성률 자동 계산
4. **`한눈에보기`** — 예산 vs 실적 차트  
   - 달성률 80%↑ 노랑, 100%↑ 빨강 배경

### 비상자금 (개월)

```
(현금 + CMA 평가액) ÷ 이번 달 총 지출
```

- **`한눈에보기`** KPI에 **N개월** 표시
- 일반적으로 **3~6개월** 이상을 목표로 많이 둡니다

### 목표 순자산 · 예상 달성

- **`설정`** B4 목표 금액
- **`대시보드(상세 KPI)`** — 목표 진행률, 예상 달성 개월  
  (최근 N개월 순자산 증가 평균 기반, N=`설정` B6)

---

## 자산 이동 · 템플릿

### 자산_이동 시트

| 열 | 내용 |
|----|------|
| 이동유형 | 계좌간이동, ISA만기→연금, 아파트구매… |
| 출발/도착 | 계좌유형 + 계좌명 |
| 금액 | 원 |
| 상태 | **예정** → 메뉴 실행 → **완료** |

**실행:** 재무관리 → 💸 자산 이동 → **예정 전체 실행**

### 이동_템플릿

1. `이동_템플릿` 시트에서 행 번호 확인
2. 메뉴 **템플릿 적용** → `행번호,금액` 입력 (예: `2,20000000`)
3. `자산_이동`에 자동 입력 → **실행**

### 아파트 구매 팁

1. `부채`에 담보대출 먼저 등록
2. `자산_이동` — 아파트구매, 자기자금만 이동
3. `자산_종목`에 부동산 행 추가·금액 수정

---

## 시세 · 환율 · 일별 자산 기록

### 중요: 실시간 HTS 아님

| 항목 | 내용 |
|------|------|
| 소스 | Yahoo Finance |
| 갱신 | 메뉴 클릭 또는 **매일 09:00** (트리거) |
| 지연 | 미국장 약 15~20분 |
| 환율 | USD/KRW, CNY/KRW (위안) |

### 일별 자산 (`자산_일별이력`)

- **Person1 / Person2 / 공동 / 합계** 매일 1행
- **오늘 날짜**는 갱신할 때마다 **덮어쓰기** (항상 최신)
- 선그래프는 이 시트에만 표시

---

## 재무관리 메뉴 (Apps Script)

스프레드시트 상단 **「재무관리」**:

| 서브메뉴 | 항목 | 용도 |
|----------|------|------|
| **📊 갱신** | 시세+환율+일별자산 | **가장 자주** |
| | 시세만 / 환율만 | 부분 갱신 |
| | 일별 자산 스냅샷 | 시세 없이 오늘 기록만 |
| **💸 자산 이동** | 템플릿 적용 | ISA→연금 등 |
| | 예정 전체 / 행 지정 실행 | 이동 반영 |
| **📅 리포트·알림** | 만기 임박 알림 | 7일 이내 |
| | 월말 스냅샷 | 순자산_이력 |
| | 월간 리포트 생성 | Google Docs |
| **⚙** | 트리거 설치 / 제거 | 자동 on/off |

---

## 자동 실행 (트리거)

| 시각 | 동작 |
|------|------|
| **08:00** | 만기 7일 이내 토스트 알림 |
| **09:00** | 환율 + 시세 + **일별 자산 스냅샷** |
| **23:00** | 월말이면 스냅샷 + Docs 리포트 |

설치: **재무관리 → ⚙ 트리거 설치 (자동)** (배포 후 1회)

---

## 월간 리포트 (Google Docs)

- 배포 시 **Docs 템플릿**이 자동 생성되고 `설정` B7에 ID 저장됨
- **월간 리포트 생성** 메뉴 → 이번 달 수입·지출·순자산·목표 요약 문서
- (선택) `설정` B8 **Drive 폴더 ID** — 리포트 저장 위치 지정

---

## 다른 컴퓨터에서 GitHub 받기 · Google 연동

배우자·가족·다른 PC 사용자를 위한 가이드입니다.  
**상황에 따라 필요한 작업이 다릅니다.**

### 먼저 확인: 나는 어떤 경우인가?

| 경우 | 설명 | 해야 할 일 |
|------|------|-----------|
| **A. 이미 만든 시트 쓰기** | 배우자 PC/폰에서 **같은 가계부** 입력 | Spreadsheet **URL만** (아래 [A](#a-이미-배포된-시트-쓰기-가장-흔함)) |
| **B. 내 PC에서 처음 deploy** | GitHub 코드 받아 **새 Google 시트** 생성 | clone + OAuth + deploy (아래 [B](#b-github-받아-처음-google-연동--deploy)) |
| **C. 코드만 받아 수정** | 개발·업데이트 목적 | clone + `credentials.json` + deploy |

---

### A. 이미 배포된 시트 쓰기 (가장 흔함)

**Git clone · Python · deploy 필요 없음**

1. 시트를 만든 사람에게 **Spreadsheet URL** 받기  
   (예: `https://docs.google.com/spreadsheets/d/xxxxx/edit`)
2. **같은 Google 계정**이면 URL만 열면 됨
3. **다른 Google 계정**(배우자 Gmail)이면:
   - 시트 소유자가 **공유** → 배우자 이메일 추가 → **편집자** 권한
   - 배우자는 링크로 접속 후 지출·수입·자산 입력

| 할 일 | 필요 |
|------|------|
| 매일 지출·수입 입력 | URL + (공유된) Google 계정 |
| 시세 갱신·트리거 | 시트 **편집 권한** + 메뉴 「재무관리」 |
| `python setup.py deploy` | **불필요** |

> 데이터는 **Google Drive(클라우드)**에 있으므로, 다른 PC에서도 URL만 있으면 동일한 시트를 씁니다.

---

### B. GitHub 받아 처음 Google 연동 + deploy

**「내 컴퓨터에서 직접 Google에 시트를 만들고 싶다」** 할 때 (가구당 보통 **1번만**)

#### B-1. 저장소 받기

```powershell
git clone https://github.com/moonhyun-cheol/money-morny.git
cd money-morny
```

#### B-2. Python 설치

- [python.org](https://www.python.org/downloads/) → **3.10+** 설치
- 설치 시 **「Add Python to PATH」** 체크

확인:

```powershell
python --version
# 또는
py -3 --version
```

#### B-3. 패키지 설치

```powershell
pip install -r requirements.txt
```

#### B-4. Google Cloud 연동 파일 (`credentials.json`)

GitHub에는 **올라가 있지 않습니다** (보안). 아래 **셋 중 하나**로 준비:

| 방법 | 설명 |
|------|------|
| **① 공유** | 이미 deploy한 사람에게 `config/credentials.json` 파일을 **USB·암호화 메신저**로 전달받음 |
| **② 직접 발급** | [처음 배포하기 Step 1~4](#step-1-google-cloud-프로젝트-만들기) 를 **본인 Google Cloud**에서 새로 설정 |
| **③ 같은 가구** | 한 사람만 Cloud 설정 → `credentials.json`을 가족 PC에 복사 (deploy는 한 PC에서만 해도 됨) |

파일 위치 (프로젝트 루트 기준):

```
money-morny/config/credentials.json
```

`credentials.json.example`과 **이름·형식**을 참고하되, 내용은 Cloud Console에서 받은 **실제 JSON**이어야 합니다.

#### B-5. Google 계정 연동 (deploy 실행)

```powershell
cd money-morny
python setup.py deploy
```

**화면에서 일어나는 일:**

1. 터미널에 `Google OAuth 인증 중...` 출력
2. **브라우저 자동 실행** → Google 로그인
3. **「Google 계정으로 로그인」** — deploy에 쓸 Gmail 선택
4. **권한 요청** 화면 → **허용** (Sheets, Docs, Drive, Script)
5. `인증 완료` 후 터미널로 돌아와 배포 진행
6. 완료 시 **Spreadsheet URL** 출력

> Cloud Console **테스트 사용자**에 이 Gmail이 등록돼 있어야 합니다.  
> `access blocked` 오류 → [FAQ](#oauth-access-blocked--403) 참고.

첫 deploy 성공 시 `config/token.json`이 **자동 생성**됩니다.  
같은 PC에서 다시 deploy할 때는 브라우저 없이 로그인될 수 있습니다.  
**`token.json`도 Git에 올리지 마세요.**

#### B-6. Spreadsheet에서 Google Apps Script 연동

deploy 직후 **Spreadsheet**를 열고:

1. **F5** 새로고침
2. 상단 **「재무관리」** 메뉴 확인
3. **재무관리 → ⚙ 트리거 설치 (자동)** 클릭
4. **Apps Script 권한** 추가 승인 (최초 1회, Google 보안 경고 → 고급 → 허용)

이 단계까지 해야 **매일 9시 시세·일별 자산·월말 리포트**가 동작합니다.

#### B-7. 배포 직후 설정

[배포 직후 10분 설정](#배포-직후-10분-설정)과 동일합니다.

---

### C. 두 사람이 각자 PC를 쓸 때 (2인 가구 권장)

| 역할 | PC에서 할 일 |
|------|-------------|
| **한 명 (관리자)** | GitHub clone → `credentials.json` → **deploy 1회** → URL·공유 설정 |
| **다른 한 명** | **공유받은 URL**만 즐겨찾기 → Sheets 앱/브라우저로 입력 |

- **새 deploy를 두 번** 하면 **시트가 2개** 생깁니다. 가구당 **시트 1개**를 권장합니다.
- 배우자 PC에 Git·Python이 없어도 **일상 사용은 가능**합니다.

---

### D. 코드 업데이트 (GitHub에 새 버전 올라왔을 때)

```powershell
cd money-morny
git pull
```

| 변경 내용 | 조치 |
|----------|------|
| Apps Script만 변경 | 시트 **Apps Script 편집기**에 `.gs` 파일 수동 반영, 또는 재deploy |
| 시트 구조 변경 | `python setup.py deploy` → **새 시트** 생성 (기존 데이터 백업 필수) |

---

### E. Google 연동 체크리스트 (다른 PC)

**deploy 담당 PC:**

- [ ] Git clone 또는 ZIP 다운로드
- [ ] Python 3.10+ · `pip install -r requirements.txt`
- [ ] `config/credentials.json` 준비
- [ ] Cloud Console 테스트 사용자에 Gmail 등록
- [ ] `python setup.py deploy` → URL 저장
- [ ] Spreadsheet → 트리거 설치 · Apps Script 권한

**매일 입력만 하는 PC/폰:**

- [ ] Spreadsheet URL (또는 공유 초대 수락)
- [ ] Google Sheets 앱 또는 브라우저

---

## 다른 기기에서 사용하기 (요약)

| 목적 | 필요한 것 |
|------|-----------|
| **매일 입력·조회** | Spreadsheet **URL** (공유된 Google 계정) |
| **처음 deploy** | [B. GitHub + Google 연동](#b-github-받아-처음-google-연동--deploy) |
| **코드 수정** | clone + `credentials.json` |

배포 후 **데이터는 Google Drive(클라우드)**에 있습니다.  
로컬 `money-morny` 폴더는 **deploy·코드 수정할 때만** 필요합니다.

---

## 문제 해결 (FAQ)

### 「재무관리」메뉴가 없어요

1. Spreadsheet **F5 새로고침**
2. [Apps Script URL](배포 시 출력) → `Code.gs` → 함수 `onOpen` 선택 → **실행**
3. 권한 승인 후 시트로 돌아와 새로고침

### OAuth: access blocked / 403

- Cloud Console → OAuth → **테스트 사용자**에 **deploy에 쓰는 Gmail** 추가
- API 4개 모두 **사용** 설정 확인
- 다른 PC에서 처음 로그인할 때도 **같은 Gmail**이 테스트 사용자 목록에 있어야 함

### credentials.json이 없다

```
FileNotFoundError: credentials.json이 없습니다
```

→ Step 4 다시: JSON을 `config\credentials.json`으로 저장

### 시세 갱신 실패

- 티커·시장(US/KS)·통화(USD/KRW) 확인
- 실패 종목은 **H열 현재가** 수동 입력

### 담당자별 금액·차트가 0

- **`설정` B2/B3 이름**과 **`자산_종목` C열**이 **완전히 동일**한지 확인  
  (예: `사람1` ≠ `사람 1`)

### 순자산·DSR이 이상해요

- `부채` D열 **현재잔액** 확인
- `자산_종목` I열(원화 평가) 비어 있으면 **시세+환율 갱신**

### deploy를 다시 실행하면?

→ **새 스프레드시트**가 생깁니다. 기존 데이터는 **복사되지 않습니다**.  
Drive에서 기존 파일 백업 후 진행하세요.

---

## 보안 · 재배포 주의

**절대 공유·Git 업로드 금지:**

- `config/credentials.json` — OAuth 클라이언트
- `config/token.json` — 로그인 토큰

**재배포 (`python setup.py deploy`):**

- 새 Sheets + 새 Docs + 새 Script 생성
- 기존 URL과 **별개** 파일

**코드만 수정했을 때:**

- Apps Script 편집기에서 `.gs` 파일 수동 붙여넣기 가능
- 시트 구조 변경은 **재배포**가 필요

---

## 빠른 참조 — 첫 배포 체크리스트

- [ ] GitHub clone (`git clone https://github.com/moonhyun-cheol/money-morny.git`)
- [ ] Cloud 프로젝트 생성
- [ ] API 4개 활성화
- [ ] OAuth 테스트 사용자 등록
- [ ] `config/credentials.json` 저장
- [ ] `pip install -r requirements.txt`
- [ ] `python setup.py deploy`
- [ ] Spreadsheet URL 저장 · (배우자에게 공유)
- [ ] 설정: 이름·목표·예산
- [ ] 트리거 설치
- [ ] 자산_종목 입력
- [ ] 시세+환율+일별자산 1회 실행
- [ ] 한눈에보기 확인

---

*문서 버전: 2026-06 · 템플릿 19탭 · UX 허브 `한눈에보기`*
